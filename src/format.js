define(function() {
    return {
        'money' : function(num) {
            return num;
        },
        'int' : function(num) {
            return num;
        },
        'float' : function(num) {
            return num;
        },
        'time' : function(num, format) {
            return num;
        },
    };
});
